# Тема 1: Системы счисления, биты и байты

## 🎯 Цель
Понять представление данных в компьютере на самом низком уровне.

## 📚 Теория

### 1.1 Бит и байт
- **Бит** (bit) - минимальная единица информации (0 или 1)
- **Байт** (byte) = 8 бит = минимальная адресуемая единица памяти

### 1.2 Системы счисления

#### Двоичная (binary)
- Основание: 2
- Цифры: 0, 1
- Пример: `1011₂ = 1×2³ + 0×2² + 1×2¹ + 1×2⁰ = 11₁₀`
- В C++: `0b1011`

#### Шестнадцатеричная (hexadecimal)
- Основание: 16
- Цифры: 0-9, A-F
- 1 hex цифра = 4 бита (полбайта)
- Пример: `FF₁₆ = 255₁₀`
- В C++: `0xFF`

### 1.3 Представление целых чисел

#### Беззнаковые (unsigned)

Диапазон для N бит: `0 ≤ N ≤ 2ᴺ - 1`
uint8_t: 0..255

#### Знаковые (two's complement)

Диапазон для N бит: `-2ᴺ⁻¹ ≤ N ≤ 2ᴺ⁻¹ - 1`
int8_t: -128..127

**Формула two's complement:**

-X представлено как 2ᴺ - |X|
Пример: `-42 в 8 битах → 256 - 42 = 214 = 0xD6`

### 1.4 Порядок байтов (Endianness)

- **Little-endian** (x86, ARM): младший байт по младшему адресу
- 0x12345678 → в памяти: `78 56 34 12`
- **Big-endian** (сетевой порядок): старший байт по младшему адресу

## 💻 Практические задачи

### Задача 1: Конвертер систем счисления
Реализовать утилиту для перевода чисел между системами.

### Задача 2: Hex-дамп памяти
Показать содержимое области памяти в формате hex.

### Задача 3: Анализатор представления чисел
Показать битовое представление числа.

### Задача 4: Анализатор битового представления
Показать точное битовое представление любого типа данных в памяти.

### Задача 5: Детектор порядка байтов (Endianness)
Определить, является ли система little-endian или big-endian.

### Задача 6: Калькулятор two's complement
Рассчитывать представление отрицательных чисел в дополнительном коде.

Запусти demo.cpp:

```
cd src/low_level/number_systems
g++ -std=c++17 -I./include -o demo.exe examples/demo.cpp src/converter.cpp
./demo.exe
```